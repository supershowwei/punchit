
@{
    ViewBag.Title = "PunchIt";
}

<div class="row">
    <div class="col-xs-12" style="height:380px;">&nbsp;</div>
</div>
<div class="row">
    <div class="col-xs-4"></div>
    <div class="col-xs-4"></div>
    <div class="col-xs-4">
        <h2>上班</h2>
        <input class="btn btn-default" id="punchButton" type="button" value="打卡" />
        <h4 id="punchedMessage"></h4>
    </div>
</div>
@section scripts {
    <script src="~/Scripts/jquery.signalR-2.2.0.min.js"></script>
    <script src="~/signalr/hubs"></script>
    <script>
        $(function () {
            // 宣告 SignalR Hub
            var punchHub = $.connection.punchHub;

            // 宣告 Client 端可以被 Server 呼叫的方法
            punchHub.client.punched = function () {
                console.log('打卡成功！');
                $('#punchedMessage').text('打卡成功！');
            };

            // 開始與 SignalR 伺服器連線
            $.connection.hub.start()
                .done(function () {
                    $('#punchButton').click(function () {
                        punchHub.server.punch();
                    });
                });

        });
    </script>
}
